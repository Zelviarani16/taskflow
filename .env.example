APP_ENV=localhost
PORT=8000

DB_HOST=localhost
DB_USER=postgres
DB_PASS=postgres
DB_PORT=5432
DB_NAME=taskflow_dev
DB_SSLMODE=disable

# Any long random string works locally. Use a real secret in production.
JWT_SECRET=super-secret-dev-key-change-me
